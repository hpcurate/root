# v3.0.0 — CREATE, and two things that were the same mistake twice

**Project:** root
**Date:** 2026-09-06
**Bump:** major (2.26.2 → 3.0.0)
**Commit:** `6f695a9` (`6f695a9c4be2fbf075b07d0c31a280dd487233a4`)

## Request

**Source:** Todoist, 4 tasks labelled `@claude` (fetched 2026-09-05, re-fetched 2026-09-06)

| # | Type | Tab | Request | Task |
| - | ---- | --- | ------- | ---- |
| 1 | feature | other | major update : new tab . “CREATE” . for music production and mixing, do what you think is best first then we will adjust | `6hR5PfG7w8GFxM2p` |
| 2 | fix | other | title tab name dot shadow is still not correct, it needs to be the color of the text in the title. basically shadow and text colour are inverted for the dot | `6hR692fvJj5mgHgG` |
| 3 | fix | store | fix colour effect when store total goes up or down, it needs to change all the colours to green/red on tap (numbers/decimal point/shadow/currency symbol) they need to be linked and have the same speed of animation. also add a typwriter still animation where the texts numbers change like a airport flight times board | `6hR69WHphj5VWMgG` |

**Parked, not built** — read, listed, and left alone:

| Type | Tab | Request | Task |
| ---- | --- | ------- | ---- |
| idea | other | bring content down: triple tap screen to bring elements down 50% to make it more reachable from my hands on the screen | `6hR4FC9M2QPPjx8G` |

Tasks 2 and 3 were filed after the run had started. One run is one template is
one version, so they fold into this one rather than becoming 3.0.1 and 3.0.2 —
which turned out to be the right call for a second reason: they share a root
cause with each other.

### The filled template

**Bump:** major. The CREATE task asks for one in words ("major update"), which
is the only way a major is ever taken. The two fixes are patches and are
carried by it.

**Headline:** CREATE — a tenth tab for music production and mixing, and two
fixes that were the same mistake twice.

**Why:** the dot task says the last fix did not work ("still not correct"). The
store task says what is wrong with the current flash: the parts of the total
change colour independently instead of together.

**Where:** `other` and `store`. CREATE being a new app, the shell, the settings
menu, search and the app list are touched as well as the new tab itself.

**Done when:**

- CREATE is the tenth app: its own tab, slide, stylesheet, module, settings
  panel and content editors, reachable on an install that already has an app list.
- A song can be added, moved through its stages, ticked off against that stage's
  checklist, given notes, and have studio sessions logged against it.
- The dot after a wordmark actually wears a title-coloured shadow, rather than
  only being declared to.
- The digits, the decimal point, the shadow and the currency symbol of STORE's
  total go green or red together, at one speed.
- A digit that changes turns over like a departure board.
- Nothing about the other nine apps changes.

**Notes:** the CREATE task says "do what you think is best first then we will
adjust", so the shape of the app is Claude's call.

---

## What was done

### CREATE — the songs being made *(task 1)*

The brief was one line, so the shape of the app is a decision. This is it:

> **A song sits on a stage, a stage asks a checklist of it, and every hour at
> the desk is written down.** Three screens — the shelf, one song, the session
> log — and no network at all.

**Why that and not a task list.** ROOT already has four ways to track work that
is due and none of them fit a song: a song has no date, it is never late, and
having six open at once is the normal state of the craft rather than a backlog
to clear. What is actually hard to hold in your head is *which one is at which
point*, and what "mix" means at four in the morning when you have already heard
it two hundred times. So the stage is the state, and the checklist is the app
remembering the discipline you are too close to the track to remember. Nothing
is due, nothing badges the tab, nothing is ever overdue.

**Split along the 2.0 line.** Config (`create.*`) holds the stages, their
colours and their checklists — the whole path a song walks is editable from
Settings → create, including adding stages and rewriting every list. The module
holds no list of its own: the strip on the shelf, the stepper and the checklist
are all drawn from Config, so a stage added in the editor needs no code and no
CSS. `create_v1` holds the songs, their ticks, their notes and every session.

**The screens.**

- *The shelf* — how many songs are in flight, a strip of one segment per stage
  as wide as the songs on it, the songs (sorted by last worked on, by stage or
  by name), the last seven days' hours, and a door to the log. Released songs
  are behind a fold.
- *One song* — tempo, key and tags as chips that open the app's own dialog; the
  stage stepper; that stage's checklist; notes saved as you type; and the log
  form — hours through the numpad's `duration` kind (130 is 1h30), a word about
  what you did, and the session kinds as chips that *fill* the field rather than
  replacing it.
- *The session log* — every session under the day it happened on, with the week
  and all-time totals.

**Decisions made on Hugo's behalf**, since "we will adjust" starts from here:

- **Six stages ship** — idea, sketch, arrangement, mix, master, released — with
  a checklist each. They are a starting point, not a claim about how you work;
  all of it is editable.
- **A tick is filed under `stageKey|item text`.** Reordering a checklist keeps
  every tick; rewording a line drops that one line's. A key column in the editor
  would have protected the rename and would have been worse to live with than
  the thing it protects — the same call DO's routine items already make.
- **The finished stage is found by `terminal`, never by its key or its
  position**, so "released" can be renamed to anything and the shelf still knows
  where a song stops. Delete the row carrying it and the last stage inherits it.
- **No Todoist, no badge, no due date.** Deliberate, per the reasoning above.
- **The icon is a waveform**, five bars. A fader would have been the settings
  sliders again.
- **It ships last in the bar**, before settings, so no existing tab moves
  position and the keyboard's `1`–`9` still land where they did.

**Wired exactly as §7 says and nowhere else:** a slide whose `#s-home` starts
with a `.h-top`, `css/create.css` scoped to `.ns-create`, `js/create.js` calling
`Shell.register('create')`, a `.tab-b` and a `tab-create` sprite symbol,
`'create'` in `Prefs.APPS`, a settings panel ending in its content box,
`SET.PANELS` / `CATS.apps` / `SEG_NAMES` / `RENDERERS` / `APP_NAMES` /
`APP_HINTS` / `GROUPS`, two `EDITORS` entries with their `EDITOR_ORDER` rows, and
two `CONTENT` rows plus a `search` hook so both the stages and the songs are
findable. The `appsSeen` migration carries it onto an install that already has an
app list — the trap CAL walked into, and there is a check for it.

### The dot's shadow, properly this time *(task 2)*

2.26.2 said the dot after a wordmark takes the title colour as its shadow. It
did not, and Hugo was right that it was "still not correct".

`.h-logo em{--title-sh-c:var(--tx)}` was the whole of that fix, on the reasoning
— written into the comment — that `text-shadow` is inherited so restating the
colour was enough. **A `var()` is substituted on the declaration that uses it,
on the element that declaration applies to.** `.h-logo`'s `text-shadow` had
already resolved to the accent; what the dot inherited was that finished value,
with no custom property left in it to override. The dot went on wearing an
accent shadow behind an accent glyph — not a shadow, just a slightly fatter dot,
the exact non-effect 2.26.2 was written to remove. For two versions.

The rule re-declares `text-shadow` beside the property now. STORE's counter and
its currency mark have always done both, which is why those two worked and this
one did not; §6 now states it in the general form.

**The check that passed was reading the custom property alone.** jsdom does not
cascade, so a regex on the stylesheet is all there is — and that one asserted
the half that was present rather than the half that made it work. It reads the
declaration beside it now, plus a second check for the re-declaration on its own.

### STORE's total: one signal, one speed, and a board *(task 3)*

Same root cause, one level along.

The counter was rebuilt with an `innerHTML =` on every repaint, which threw the
currency mark away and built a new one. **A replaced element has no previous
computed value, so it cannot transition:** the digits eased into green over
`--dur-3` while the mark *snapped* to it. One signal arriving twice at two
speeds — exactly the "they need to be linked and have the same speed" the task
asks for.

- The number is **cells** now, one per character with the decimal point
  included, updated in place; the mark is **moved** across a rebuild rather than
  recreated.
- The cells declare no colour and no shadow of their own, on purpose: an
  inherited value follows the parent's transition frame by frame, so the digits,
  the point and the shadow move with `.h-cost` for free and cannot drift from
  it. The mark is the single exception, because it is deliberately the other
  colour, and it carries a matching transition so it arrives with them.
- **The cards turn over.** A character that changes flips in from the top edge
  in a short perspective, each cell a beat behind the one to its left — a
  departure board. Only the characters that actually changed get it, so
  4.50 → 4.90 turns one card rather than four, which is what makes a board read
  as fast as it does. It rides `--mo`, so "no motion" stops it dead. Arriving is
  still not a change: pinning the counter mid-trip mounts, and the cells do not
  also flip.

### Two things not asked for

Both called out because they were not in any task.

- **The colour-coded tab list had no entry for DAY.** Adding CREATE's meant
  writing the line next to the gap — DAY was the only app whose pill fell back
  to nothing with colour-coded tabs on. One line.
- **A harness check failed for one hour every night.** DAY's now-line fixture
  built its day from `hour - 1` modulo 24, so between 00:00 and 00:59 it wrote a
  day starting at 23:00 — an hour before the date it was filed under — then
  asked CAL where midnight fell in a day that had not started. CAL was right
  both times; the fixture was wrong. The window slides to fit now instead of
  wrapping. Found by running the suite at 00:14, and it is why this version's
  first green run took three attempts to explain.

### Documentation

`ROOT.md`: §1 (nine tools, CREATE's row), §2 (both new files, both load orders),
§3 (the module list, and CREATE as the third `data-act` app), §5 (`create_v1`),
§6 (five new entries — the `var()` substitution rule, the element that cannot
transition, CREATE's tick key, its `terminal` flag, and a deleted stage), §7
(CREATE as the worked example for adding a tab, and the check count), and the
3.0 changelog entry.

## Verification

`test/harness.mjs` — **755 checks, all green** (was 524 documented / 748 after
CREATE alone), **31 new**:

- CREATE: the wiring, the empty shelf, starting a song through the app's own
  dialog, the Config-driven checklist, a tick surviving a checklist reorder, the
  stage stepper, a session logged and the shelf's week reading it, a stage added
  in the editor reaching the stepper, a stage deleted from under a song, search
  finding a song, the released fold, the delete that takes its sessions with it,
  both content editors, and the `terminal` flag surviving an add and a delete.
- The dot: the re-declared `text-shadow`, and a second check for it alone.
- The counter: the currency mark being the same node after a repaint, the cells
  matching the total, the cells carrying no colour of their own, only changed
  characters flipping, and the animation's shape and its `--mo`.

Also updated rather than added: the tab-count checks (nine → ten), the shipped
order, the settings pill bar, the apps-out-of-the-bar list, the band-shape and
app-sheet lists, and the `appsSeen` migration's expected result.

**Not verified by eye.** jsdom does not lay out or paint, and the browser
extension was not connected this session, so nothing in this version has been
seen on a screen: CREATE's whole appearance, the dot's shadow and the flip board
are argued from the stylesheet rather than looked at. A local server was started
for exactly that and could not be driven.

## Deferred

Nothing. The `@idea` task (`6hR4FC9M2QPPjx8G`) is parked by definition, not
deferred: listed above, left open in Todoist, and not built.
